[project]
name = 'bigview-project'
description = 'DeFi Treasury and Staking Protocol'
authors = ['Henry Goodluck Chigozie']
telemetry = true
cache_dir = './.clarinet'

# 2. Your Token (Now depends on the remote requirement)
[contracts.bigview-token]
path = 'contracts/bigview-token.clar'
clarity_version = 2
epoch = '2.5'
depends_on = [] # Removed local dependency

# 3. The Treasury (Depends on the others)
[contracts.bigview-treasury-v8]
path = 'contracts/bigview-treasury-v8.clar'
clarity_version = 2
epoch = '2.5'
depends_on = ['bigview-token']

# 4. ADD THIS NEW SECTION
[requirements]
[[requirements.contracts]]
source = "SP3FBR2AGK5H9QBDH3EEN6DF8EK8JY7RX8QJ5SVTE.sip-010-trait-ft-standard"

[repl.analysis]
passes = ['call_checker', 'check_checker']